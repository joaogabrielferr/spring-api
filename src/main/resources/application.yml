spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring-api
    username: root
    password: 1234567
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: false

  application:
    name: USER-SERVICE

springdoc:
  pathsToMatch: /api/**/**
  swagger-ui:
    use-root-path: true